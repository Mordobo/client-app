[build]
  command = "npm install && npx expo export --platform web"
  publish = "dist"

[build.environment]
  NODE_VERSION = "20.19.4"
  CI = "true"

[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
